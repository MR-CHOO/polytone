/**
 * Resolver test/demo material: the {@code ExamplePages} list of exercised codecs (demo
 * records, raw vanilla codecs, coverage checks, stress tests) plus the migrated-example
 * classes they reference. The production editor
 * ({@code net.mehvahdjukaar.polytone.editor.PolytoneEditor}) appends these pages to its
 * library in dev environments only; nothing else may reference this package. (It may peek
 * at {@code internal} for debug assertions — it's test code.)
 */
package net.mehvahdjukaar.polytone.common.codec_ui.example;
