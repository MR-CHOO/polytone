/**
 * Demo / manual-test material: the standalone {@code ExamplesLauncher} Swing app, the
 * {@code CodecRegistry} list of exercised codecs, migrated real-world examples and sample
 * custom widgets. Nothing in production code may reference this package; it exists to
 * exercise the resolver on progressively nastier codecs. (It may peek at {@code internal}
 * for debug assertions — it's test code.)
 */
package net.mehvahdjukaar.polytone.common.codec_ui.example;
